SECRET_KEY=change-me
DEBUG=True
# Optional: enables the student-only Nova AI endpoint. Never commit a real key.
GEMINI_API_KEY=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# Must exactly match the authorized redirect URI configured in Google Cloud.
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/accounts/google/callback/
